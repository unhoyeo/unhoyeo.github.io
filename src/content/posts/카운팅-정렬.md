---
title: "카운팅 정렬"
description: "공사 중인 글입니다."
pubDate: 2025-03-12T22:37:06+09:00
category: "알고리즘/개념"
tags: []
draft: true
---

> 공사 중인 글입니다.

```java
public class Main {

    static int MV = 1_000_000_000;

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int N = Integer.parseInt(br.readLine());
        int[] arr = new int[N];
        boolean[] brr = new boolean[MV * 2 + 1];

        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            int n = Integer.parseInt(st.nextToken()); // -MV ~ MV
            brr[n + MV] = true;
        }

        for (int i = 0, j = 0; i < brr.length; i++) {
            if (brr[i]) {
                arr[j++] = i - MV;
            }
        }
    }
}
```
