---
title: "2563﻿번 색종이"
description: "가로, 세로의 크기가 각각 100인 정사각형 모양의 흰색 도화지가 있다. 이 도화지 위에 가로, 세로의 크기가 각각 10인 정사각형 모양의 검은색 색종이를 색종이의 변과 도화지의 변이 평행하도록 붙인다."
pubDate: 2025-02-25T19:40:51+09:00
category: "알고리즘/백준"
tags: []
draft: true
---

<https://www.acmicpc.net/problem/2563>

## 문제

가로, 세로의 크기가 각각 100인 정사각형 모양의 흰색 도화지가 있다. 이 도화지 위에 가로, 세로의 크기가 각각 10인 정사각형 모양의 검은색 색종이를 색종이의 변과 도화지의 변이 평행하도록 붙인다. 이러한 방식으로 색종이를 한 장 또는 여러 장 붙인 후 색종이가 붙은 검은 영역의 넓이를 구하는 프로그램을 작성하시오.

![2563﻿번 색종이](/images/tistory/21/img.png)

예를 들어 흰색 도화지 위에 세 장의 검은색 색종이를 그림과 같은 모양으로 붙였다면 검은색 영역의 넓이는 260이 된다.

## 입력

첫째 줄에 색종이의 수가 주어진다. 이어 둘째 줄부터 한 줄에 하나씩 색종이를 붙인 위치가 주어진다. 색종이를 붙인 위치는 두 개의 자연수로 주어지는데 첫 번째 자연수는 색종이의 왼쪽 변과 도화지의 왼쪽 변 사이의 거리이고, 두 번째 자연수는 색종이의 아래쪽 변과 도화지의 아래쪽 변 사이의 거리이다. 색종이의 수는 100 이하이며, 색종이가 도화지 밖으로 나가는 경우는 없다

## 출력

첫째 줄에 색종이가 붙은 검은 영역의 넓이를 출력한다.

## 예제 입력

```
3
3 7
15 7
5 2
```

## 예제 출력

```
260
```

---

## 1. 문제 분석

- 100\*100 크기의 도화지 위에 10\*10 크기의 색종이를 붙인다.
- 색종이는 겹쳐서 붙일 수 있다.
- 첫째 줄에 색종이 수를 입력받고, 이어서 한 줄씩 색종이를 붙일 위치를 X공백Y 형태로 입력받는다.
- X는 도화지와 색종이 왼쪽 변 사이의 거리, Y는 아래쪽 변 사이의 거리이다.
- 즉, (X, Y)는 색종이의 왼쪽 아래쪽의 꼭짓점의 좌표다.
- 색종이 수는 100 이하, 도화지 밖으로 나가는 경우는 없다.

## 2. 풀이 고안

- 첫째 줄에서 정수를 입력받아 int 변수 N에 저장한다.
- 100\*100 크기의 int타입 2차원 배열을 생성한다.
- 값이 0이면 색종이가 없는 것이고 1이면 색종이가 있는 것이다.
- 색종이를 다 붙이고 배열의 원소들을 다 더하면 색종이 넓이가 나온다.
- 색종이의 좌표(X, Y)를 입력받고, 배열의 인덱스 [X][Y]부터 [X+9][Y+9]까지 for문을 이용해 1로 만든다.
- 해당 과정을 for문 i < N조건으로 반복하고, 끝나면 배열의 원소를 다 더하여 출력한다.

**3. 효율성 검증**

- 시간제한은 1초이므로 약 10^8(1억)번의 연산 안에 완료해야 한다.
- 이중 for문을 사용했으므로 시간복잡도는 O(N\*10\*10)이다.
- N은 최대 100이므로, O(100\*10\*10=10^4)이다.
- 널널하게 통과할 수 있다.

**4. 풀이**

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int N = Integer.parseInt(br.readLine());
        int[][] arr = new int[100][100];
        for (int i = 0; i < N; i++) {
            StringTokenizer st = new StringTokenizer(br.readLine());
            int X = Integer.parseInt(st.nextToken());
            int Y = Integer.parseInt(st.nextToken());
            for (int j = 0; j < 10; j++) {
                for (int k = 0; k < 10; k++) {
                    arr[X + j][Y + k] = 1;
                }
            }
        }
        int result = 0;
        for (int[] row : arr) {
            for (int r : row) {
                result += r;
            }
        }
        System.out.println(result);
    }
}
```
