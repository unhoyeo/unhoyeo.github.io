---
title: "2470번 두 용액 - 투 포인터"
description: "KOI 부설 과학연구소에서는 많은 종류의 산성 용액과 알칼리성 용액을 보유하고 있다. 각 용액에는 그 용액의 특성을 나타내는 하나의 정수가 주어져있다."
pubDate: 2025-03-12T23:32:13+09:00
category: "알고리즘/백준"
tags: []
draft: true
---

<https://www.acmicpc.net/problem/2470>

KOI 부설 과학연구소에서는 많은 종류의 산성 용액과 알칼리성 용액을 보유하고 있다. 각 용액에는 그 용액의 특성을 나타내는 하나의 정수가 주어져있다. 산성 용액의 특성값은 1부터 1,000,000,000까지의 양의 정수로 나타내고, 알칼리성 용액의 특성값은 -1부터 -1,000,000,000까지의 음의 정수로 나타낸다.

같은 양의 두 용액을 혼합한 용액의 특성값은 혼합에 사용된 각 용액의 특성값의 합으로 정의한다. 이 연구소에서는 같은 양의 두 용액을 혼합하여 특성값이 0에 가장 가까운 용액을 만들려고 한다.

예를 들어, 주어진 용액들의 특성값이 [-2, 4, -99, -1, 98]인 경우에는 특성값이 -99인 용액과 특성값이 98인 용액을 혼합하면 특성값이 -1인 용액을 만들 수 있고, 이 용액이 특성값이 0에 가장 가까운 용액이다. 참고로, 두 종류의 알칼리성 용액만으로나 혹은 두 종류의 산성 용액만으로 특성값이 0에 가장 가까운 혼합 용액을 만드는 경우도 존재할 수 있다.

산성 용액과 알칼리성 용액의 특성값이 주어졌을 때, 이 중 두 개의 서로 다른 용액을 혼합하여 특성값이 0에 가장 가까운 용액을 만들어내는 두 용액을 찾는 프로그램을 작성하시오.

```java
5
-2 4 -99 -1 98
```

첫째 줄에는 전체 용액의 수 N이 입력된다. N은 2 이상 100,000 이하이다. 둘째 줄에는 용액의 특성값을 나타내는 N개의 정수가 빈칸을 사이에 두고 주어진다. 이 수들은 모두 -1,000,000,000 이상 1,000,000,000 이하이다. N개의 용액들의 특성값은 모두 다르고, 산성 용액만으로나 알칼리성 용액만으로 입력이 주어지는 경우도 있을 수 있다.

```java
-99 98
```

첫째 줄에 특성값이 0에 가장 가까운 용액을 만들어내는 두 용액의 특성값을 출력한다. 출력해야 하는 두 용액은 특성값의 오름차순으로 출력한다. 특성값이 0에 가장 가까운 용액을 만들어내는 경우가 두 개 이상일 경우에는 그 중 아무것이나 하나를 출력한다.

---

## ✅ 아이디어

- **두 숫자를 더해서 0에 가장 가까운 값**을 찾는 문제
- **오름차순 정렬 후, 투 포인터 활용**해서 찾기
  - 왼쪽 포인터 (L): **가장 작은 값**부터 시작
  - 오른쪽 포인터 (R): **가장 큰 값**부터 시작
- 현재 두 값의 합을 계산하며 **0에 가까운 값 갱신**
- **합이 음수면 L 증가, 양수면 R 감소**
- **L == R 될 때까지 반복!**

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int N = Integer.parseInt(br.readLine());

        int[] arr = new int[N];

        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }

        Arrays.sort(arr);

        int minAbs = Integer.MAX_VALUE;
        int L = 0, R = arr.length - 1;
        int minL = L, minR = R;

        while (L < R) {
            int sum = arr[L] + arr[R];
            int abs = Math.abs(sum);

            if (abs < minAbs) {
                minAbs = abs;
                minL = L;
                minR = R;
                if (sum == 0) break;
            }

            if (sum < 0) L++;
            else R--;
        }

        System.out.println(arr[minL] + " " + arr[minR]);
    }
}
```

## ⏱️ 시간 복잡도

- 정렬: O(N log N)
- 투 포인터 탐색: O(N)
- 총 시간 복잡도: O(N log N) → (N ≤ 100,000이므로 10⁵ log(10⁵) ≈ 10⁶ → 충분히 가능)

---

## ❌ 틀린 코드

```java
for (int i = 0; i < N - 1; i++) {
    for (int j = N - 1; j > i; j--) {
        int gap = Math.abs(arr[i] + arr[j]);
        if (gap == 0) {
            result = new int[]{arr[i], arr[j]};
            sb.append(result[0]).append(' ').append(result[1]);
            System.out.println(sb);
            return;
        }
        if (min < gap) break;
        min = gap;
        result = new int[]{arr[i], arr[j]};
    }
}
```

## 코드의 문제점

```java
if (min < gap) break;
```

- gap이 현재 min보다 크다면 즉시 break로 중단함
- 하지만 이 방식은 **모든 조합을 비교하는 것이 아니라, 특정 케이스를 무시**하는 문제가 발생
- **즉, 더 작은 gap을 찾을 기회를 놓칠 수 있음!**

```java
[-5, -4, 1, 4, 6]
```

예를 들어, 다음과 같은 배열이 주어지면 첫번째 턴에 -5, 6으로 **min = 1**이기 때문에 -4, 6에서 break되어 **-4, 4의 기회를 놓치게 된다.**
